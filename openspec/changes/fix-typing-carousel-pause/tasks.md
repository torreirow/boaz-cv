## 1. Override aanmaken

- [x] 1.1 Maak `assets/scripts/pages/` directory aan en schrijf `home.js` met de gecorrigeerde TypeIt-keten (`.pause(1500).delete().pause(400)` voor alle strings)

## 2. Verifiëren

- [x] 2.1 Start `hugo server` en controleer visueel dat alle strings (inclusief de laatste) even blijven staan voor ze worden weggewist
- [x] 2.2 Controleer dat het ritme gelijkmatig is over alle strings heen

## 3. Afronden

- [ ] 3.1 Commit en push het nieuwe bestand
